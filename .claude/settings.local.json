{
  "permissions": {
    "allow": [
      "Bash(python -c \"\n# Reset the global singleton to test loading\nimport src.rag.indexer as rag_module\nrag_module._global_indexer = None\n\n# Now get indexer - should auto-load from disk\nfrom src.rag.indexer import get_rag_indexer\n\nindexer = get_rag_indexer()\nstats = indexer.get_stats()\n\nprint(f''Loaded from disk:'')\nprint(f''  Total documents: {stats[\"\"total_documents\"\"]}'')\nprint(f''  Model: {stats[\"\"model_name\"\"]}'')\nprint(f''  Incident types: {stats[\"\"incident_types\"\"]}'')\n\")"
    ],
    "deny": [],
    "ask": []
  }
}
